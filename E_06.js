// Print all palindrome numbers from 1 to 1000.

function palindrome(){
    let Num = "1000";
    inputNum=ParseInt("1000").value;
    i=inpNum.length;
    while(num != 0)
{
    reverse = (revNum * 10) + (num % 10);
    num = parseInt(num / 10);
}
    for (let j=0; j>n+1; j--)
    {
        console.log(revNum+" ");
    }
    if(inNum === revNum)
        console.log(inpNum+" is a Palindrome,");
    else
        console.log(inpNum+" is not a Palindrome,");
    
}